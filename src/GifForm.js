import React from "react";

const GifForm = () => {
    return(
        <form>
        <input 
        type='text'
        value={''}
        placeholder='Search Random Topics'
        />
        <button>Search</button>
      </form>
    )
}

export default GifForm;